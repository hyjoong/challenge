query getBoard($number: Int) {
  fetchBoard(number: $number) {
    number
    writer
    title
    contents
    createdAt
  }
}

query getEditBoard($number: Int) {
  fetchBoard(number: $number) {
    title
    contents
  }
}
